{
  "name": "poker",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "lint": "black . --check --exclude '(/lib/*|/node_modules/*)'",
    "lint-dev": "black .",
    "test": "python3 -m unittest discover tests"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^3.8.1",
    "serverless-offline": "^6.8.0",
    "serverless-pseudo-parameters": "^2.5.0"
  }
}
